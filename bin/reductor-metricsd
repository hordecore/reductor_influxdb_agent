#!/bin/bash

while sleep 5; do
	/opt/reductor_influxdb_agent/bin/reductor-metrics
	retval=$?
	[ "$retval" != '0' ] && echo $(date) error $retval && continue
	[ "$(($(date +%s) % 30))" == 0 ] && echo $(date) work fine
done
